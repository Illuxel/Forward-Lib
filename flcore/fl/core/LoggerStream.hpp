#pragma once 

#include <fstream>
#include <iostream>

#include <shared_mutex>

namespace Forward {

	/**
	 * Allows safely write data to a buffer
	 */
	class LoggerStream 
	{
	
	};
}
